Web tracker for the masses
===

### To build:

Prerequisites (Mac):

    * Run command: 'mongod' in the terminal
    * npm install
    * bower install

### To run the tests:

Use mocha for server side & server side module tests
Use index.html for client side tests
